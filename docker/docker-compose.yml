version: "3.9"

services:
  skin-ai:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - ../.env
    restart: always